---------------------------------------------------------------------
-- The scene abstraction
---------------------------------------------------------------------

Camera = {}
function Camera.shake(amount)
    shakeCamera(CAMERA, amount)
end

