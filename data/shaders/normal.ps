#version 330 core

uniform sampler2D texture;

in vec4 interpolatedColor;
in vec2 interpolatedTextCoords;
out vec4 fragmentColor;

void main() {
	fragmentColor = texture2D(texture, interpolatedTextCoords) * interpolatedColor;
}