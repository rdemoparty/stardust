#pragma once

#include <vector>
#include <string>

namespace Acidrain {
    namespace StringUtils {
        using namespace std;

        vector<string> split(const string& str, char delimiter);
    }
}